'use strict'

///////// *** Takes image ID and returns a string with the image's source
function imgIdToUrl(imgId) {
    var url = '/assets/img/images/' + imgId + '.jpg';
    return url;
} // *** End of imgIdToUrl